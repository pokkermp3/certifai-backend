# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Settings
pydantic-settings>=2.2.0

# Database — async drivers
aiosqlite>=0.20.0       # local dev / SQLite
asyncpg>=0.29.0         # production / PostgreSQL (Neon)

# Storage — async S3 client
aioboto3>=13.0.0        # production / Cloudflare R2
botocore>=1.34.0        # S3 error types (ClientError) — transitive dep of aioboto3, pinned explicitly

# PDF generation
weasyprint>=62.0

# File uploads
python-multipart>=0.0.9
qrcode[pil]>=7.4.2